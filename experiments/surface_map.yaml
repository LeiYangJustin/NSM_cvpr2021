checkpointing :
  checkpoint_path    : './outputs/neural_map/'
  prefix : 'overfit'

dataset :
  sample_path : '/SET/YOUR/PATH/HERE/'
  num_workers : 4
  num_points  : 10000
  num_epochs  : 10000

loss :
  type : 'mae'

eval_step: 100

hydra:
  run:
    dir: './'
